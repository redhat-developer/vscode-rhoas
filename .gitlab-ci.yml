stages:
  - build

image: node

before_script:
  - npm install -g vsce
  - npm install

build:
  stage: build
  script:
    - npm run lint
    - vsce package
  artifacts:
    paths:
      - "*.vsix"
  tags:
    - shared
